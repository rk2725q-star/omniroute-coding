# /fullstack — Complete Full-Stack App Builder

Build a complete production full-stack app: $ARGUMENTS

## Deliver ALL of this in 1 mega-batch:

### Backend
- REST API with full CRUD + auth (JWT)
- Database schema with indexes
- Input validation + sanitization
- Structured error responses
- Rate limiting + security headers

### Frontend
- Dark mode UI with glassmorphism aesthetic
- Google Fonts (Inter), smooth CSS animations
- Fully wired to API (fetch/axios with error handling)
- Loading, error, empty, success states
- Responsive (mobile-first)

### DevOps
- package.json with all deps pinned
- .env.example with all vars documented
- README with setup + run instructions
- One command start: npm run dev

## Execution:
Turn 1: Read existing files (parallel)
Turn 2: Write ALL files (single parallel mega-batch)
Turn 3: npm install + npm run dev — verify it starts

PRODUCTION READY. ZERO STUBS. START NOW.