# /mega-build — Full Project in 1 Pass (Antigravity Style)

You are now in MEGA-BUILD MODE. Task: $ARGUMENTS

## Execution (≤3 turns, NO exceptions):

### Turn 1 — Parallel Context Scan
Fire ALL reads simultaneously (package.json, tsconfig, existing src files, schema, env).
No sequential reads. All at once.

### Turn 2 — Mega-Batch Delivery (ALL in 1 parallel write batch)
1. Schema & Data Models — fully typed, complete
2. Backend API — full CRUD, validation, auth, error handling
3. Frontend Components — interactive, wired to API, accessible
4. Production Styles — dark mode, glassmorphism, Google Fonts, animations
5. Tests — runnable assertions, not mocks
6. Config — package.json, tsconfig, .env.example, README

### Turn 3 — Verify + Fix
Run build/test. Fix errors in same turn. Declare done only when tests pass.

## ABSOLUTE RULES:
- ZERO TODOs, ZERO stubs, ZERO placeholders, ZERO lorem ipsum
- NEVER pause and ask "shall I continue?" 
- ALL files in ONE parallel batch — never file-by-file
- Choose best-practice defaults when ambiguous — never ask
- Production code only