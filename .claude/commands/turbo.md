# /turbo — Instant Turbo Mode

Turbo mode for: $ARGUMENTS

This activates the absolute minimum-turn protocol:

- Read needed context (parallel, 1 turn)
- Write all output (parallel, 1 turn)  
- Verify (1 turn if needed, else skip)

NO preamble. NO confirmations. NO step-by-step explanation.
Just: READ → WRITE → DONE.

For ultra-simple tasks (single file change): done in 1 turn.
For medium tasks: 2 turns max.
For complex: 3 turns max.

Quality standard: production-grade despite speed.
Execute immediately.